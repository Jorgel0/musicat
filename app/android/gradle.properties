org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template. Kept false:
# we're pinned below AGP 9 (see settings.gradle.kts) specifically because
# audiotags still unconditionally self-applies the Kotlin Gradle Plugin,
# which is incompatible with builtInKotlin=true.
android.builtInKotlin=false
